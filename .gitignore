# Private planning notes (contain real paths/domains); never commit.
plans/
.DS_Store
/markdown-publish

# Local deployment config: real paths and ids live here, never in the repo.
.env
docker-compose.yaml
